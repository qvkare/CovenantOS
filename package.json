{
  "name": "covenantos",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "provider",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "dev:web": "npm run dev -w @covenantos/web",
    "dev:provider": "npm run dev -w @covenantos/provider",
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present",
    "lint": "npm run lint -ws --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
